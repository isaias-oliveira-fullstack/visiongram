/**
 * Enums para abas de perfil.
 */
export enum ProfileTab {
    Posts = 'profile-posts', // Representa a aba de posts
    Peeds = 'profile-peed', // Representa a aba de peeds
    Saved = 'profile-saved', // Representa a aba de salvos
    Tagged = 'profile-tagged', // Representa a aba de marcados
}